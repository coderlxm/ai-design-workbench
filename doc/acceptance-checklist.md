# AI 设计工作台 MVP 验收清单

> 当前实现说明：前端工作流已完整跑通；在未接入真实模型接口前，生成结果使用本地 mock 图像输出，用户仍可预览、保存与下载。

## 1. 工程与约束

- [x] 使用 Vue 3.5 + TypeScript + Vite
- [x] 使用 pnpm 作为包管理器
- [x] 已初始化 Git 仓库
- [x] 已添加基础 `.gitignore`
- [x] 项目目录按 `doc / design / image / src` 方式规划
- [x] 已按设计稿实现浅色工作台风格与三栏布局
- [x] 已在模型接入位保留 `TODO(model-api)` 注释

## 2. 页面结构

- [x] 顶部工具条包含产品主题、比例、模型选择和保存入口
- [x] 左侧流程区显示 5 个步骤及当前状态
- [x] 中间工作区展示场景、线稿、模特、最终结果等阶段内容
- [x] 右侧面板展示任务状态、prompt 编辑区和日志
- [x] 支持 `/` 工作台路由
- [x] 支持 `/records` 记录页路由

## 3. 场景输入流程

- [x] 支持从 `image/scenes` 目录自动发现本地图片
- [x] 支持本地上传场景图
- [x] 支持场景图预览
- [x] 支持主体裁剪弹窗
- [x] 支持裁剪后结果回填到工作流

## 4. 线稿流程

- [x] 支持从场景图生成极简线稿预览
- [x] 支持重新生成线稿
- [x] 线稿输出已限制为白底黑线的 mock 样式

## 5. 场景 Prompt

- [x] 支持场景 prompt 自动生成
- [x] 支持场景 prompt 手动编辑
- [x] 场景 prompt 中包含美国场景设定
- [x] 场景 prompt 可查看最终组合内容

## 6. 模特输入流程

- [x] 支持模特六视图上传
- [x] 支持正面、左侧面、右侧面视图输入
- [x] 支持背面、半身、全身视图输入
- [x] 支持删除、替换、清空模特视图
- [x] 已移除肖像授权确认勾选，模特输入流程仅校验必需视图
- [x] 对缺失视图提供提示

## 7. 最终生成与下载

- [x] 支持最终 prompt 自动组合
- [x] 支持查看最终组合 Prompt 弹窗
- [x] 支持最终图片预览
- [x] 支持最终图片下载
- [x] 支持重新生成
- [x] 支持保存到本地记录

## 8. 记录与保存

- [x] 生成记录自动保存到本地
- [x] 支持记录页查看历史结果
- [x] 支持从记录页下载历史图片
- [x] 支持刷新后保留 prompt 与历史记录

## 9. 构建验收

- [x] `pnpm install` 成功
- [x] `pnpm build` 成功

## 10. 后续接真实模型时的替换点

- [x] 线稿生成服务已预留模型接口位
- [x] 场景 prompt 反推已预留模型接口位
- [x] 最终生图已预留模型接口位

## 11. 验收口径说明

当前 MVP 的验收口径是“流程跑通 + 图片可下载 + 记录可回看 + 接口位可替换”。如果后续接入真实模型，只需要替换 `src/services/` 下的 mock 实现，不需要改动页面结构和工作流主干。
